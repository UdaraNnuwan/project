# OpenAI configuration for notebook 04 / GPT adjudication.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional Windows/Jupyter workaround if notebook execution hits secure-write permission errors.
JUPYTER_ALLOW_INSECURE_WRITES=true
